# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'TwitterAssignment' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TwitterAssignment
     pod 'TwitterKit'
     pod 'Kingfisher', '~> 6.0'

  target 'TwitterAssignmentTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TwitterAssignmentUITests' do
    # Pods for testing
  end

end
